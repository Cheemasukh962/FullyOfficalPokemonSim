/* Commented out for Railway deployment. Keeping for reference if switching back to Vercel.
{
  "buildCommand": "pip install -r requirements.txt",
  "framework": "flask",
  "outputDirectory": ".",
  "env": {
    "FLASK_ENV": "production",
    "DATABASE_URL": "@DATABASE_URL"
  }
}
*/
